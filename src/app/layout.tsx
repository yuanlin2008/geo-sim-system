import "./globals.css"

import { Liu_Jian_Mao_Cao, Noto_Sans_SC, Noto_Serif_SC } from "next/font/google"

import ClientSessionProvider from "@/components/session-provider"

const fontSans = Noto_Sans_SC({
  subsets: ["latin"],
  weight: ["100", "500", "900"],
  //variable: "--font-sans",
})

const fontSerif = Noto_Serif_SC({
  subsets: ["latin"],
  weight: ["200", "500", "900"],
  //variable: "--font_serif",
})

const fontMao = Liu_Jian_Mao_Cao({
  subsets: ["latin"],
  weight: ["400"],
  //variable: "--font_mao",
})

export const metadata = {
  title: "地缘模拟系统",
  description: "Generated by create next app",
}

function Header() {
  return (
    <nav className="container relative mx-auto bg-gray-100 p-6 text-xl ">
      地缘模拟系统
    </nav>
  )
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  console.log(fontSans)
  return (
    <html lang="en" className={fontSerif.className}>
      <body>
        <ClientSessionProvider>
          <Header />
          {children}
        </ClientSessionProvider>
      </body>
    </html>
  )
}
